// Kept for backward compatibility — language switching is in header
export function LanguageSwitcher(_props: { locale?: string; locales?: Record<string, any>; onChange?: (l: string) => void }) {
  return null;
}
